driver_source = files('daq.cpp')
project_sources += driver_source


deps = [

]

model_driver_lib = static_library('driver',
                            driver_source,
                            include_directories : inc,
                            dependencies : deps)
