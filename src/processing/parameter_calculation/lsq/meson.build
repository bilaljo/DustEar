lsq_source = files('functions.cpp')

project_sources += lsq_source


deps = [
]

lsq_lib = static_library('lsq',
                         lsq_source,
                         include_directories : inc,
                         dependencies : deps)
